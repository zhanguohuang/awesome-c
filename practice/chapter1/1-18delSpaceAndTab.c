//
// Created by bytedance on 2020/11/11.
//

#include <stdio.h>

#define MAXLINE 1000

int getLine(char line[], int lim);

int main() {
    int n;
    char line[MAXLINE];
    // 怎么区别是空行还是结束了呢
    while((n = getLine(line, MAXLINE)) > 0) {

    }
}